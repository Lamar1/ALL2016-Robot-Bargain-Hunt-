import random
import sqlite3 as sql
Author = "Lamar Gould"
def randomitem():
    try:
        con = sql.connect('ITEM DATABASE.sqlite')
        cur = con.cursor
        cur.execute(''' SELECT product FROM items ORDER BY
                        RANDOM() LIMIT 9;''')
        ItemList = []

        for row in cur:
        ItemList.append(row)
        print(row)
        #List1 is what is used in searcg algorithms
        list1 = [val for sublist in ItemList for val in sublist]

    finally:
            con.close() 
    
def randomcolour():
    """ A tuple of colours to change robot colour"""
                        
