def BucketSort(a, b, bucket, c):
    for d in range(c):
        bucket[d] = 0
    for e in range(b):
        bucket[a[e]] +=1
    e = 0
    for d in range(c):
        for f in range (bucket[c]):
            g[i]  = d
            e += 1
